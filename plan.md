# akko 実装計画

## 1. 目的

Go言語で、ファイルを暗号化・復号化するCLIツール `akko` を実装する。

基本的な操作は以下とする。

```text
akko --key <key> <input>
```

または

```text
akko <input>
Password:
```

入力ファイルが通常のファイルなら暗号化し、`akko` 独自フォーマットの暗号ファイルなら復号化する。

```text
akko --key key.txt secret.txt  # 暗号化
akko --key key.txt secret.akko # 復号化
```

`enc` / `dec` のようなサブコマンドは設けない。

---

## 2. 基本方針

### 2.1 暗号アルゴリズム

暗号アルゴリズムそのものは自作しない。

初期実装では以下を採用する。

- KDF: Argon2id
- 暗号: AES-256-GCM
- salt: 暗号学的に安全な乱数で生成
- nonce: 暗号学的に安全な乱数で生成
- 認証: AES-GCMの認証タグを使用

Goで利用する暗号・KDFライブラリは、標準ライブラリまたは実績のある公式/準公式パッケージを優先する。

### 2.2 パスワードから鍵を直接生成しない

CLIから与えられた `key` がパスワード文字列の場合、それを直接AES鍵として使用しない。

以下のようにする。

```text
password
   |
   v
Argon2id(password, salt, parameters)
   |
   v
32-byte key
   |
   v
AES-256-GCM
```

saltは秘密情報ではないため暗号ファイルのヘッダーに保存する。

### 2.3 秘密情報をコマンドライン引数に置く問題

基本CLIは要件どおり `akko --key <key> <input>` を実装する。

ただし、コマンドラインにパスワードを直接指定するとshell historyやプロセス一覧等に残る可能性があるため、以下をサポートできる設計にする。

```text
akko <input> <output>
Password:
```

またはkey-file方式。

```text
akko --key <key> <input>
```


初期実装でパスワード入力方式を追加する場合は、端末上でエコーされない入力を利用する。

---

## 3. 暗号ファイルフォーマット

暗号ファイルは自己記述型の独自バイナリフォーマットとする。

概念的には以下。

```text
+-------------------------+
| Magic                   |
+-------------------------+
| Version                 |
+-------------------------+
| Algorithm               |
+-------------------------+
| KDF                     |
+-------------------------+
| KDF Parameters          |
+-------------------------+
| Salt                    |
+-------------------------+
| Nonce / Nonce Base      |
+-------------------------+
| Original file extension |
+-------------------------+
| Encrypted data          |
+-------------------------+
```

### 3.1 Magic

ファイル先頭に固定のMagic値を置く。

例:

```text
akko
```

ただし、実際のバイナリフォーマットでは長さを固定し、曖昧な判定が起きないようにする。

Magicによって、通常ファイルと `akko` ファイルを識別する。

### 3.2 Version

フォーマットバージョンを必ず持つ。

例:

```text
Version = 1
```

将来、暗号方式やフォーマットを変更しても旧ファイルを扱えるようにする。

実装では、未知のバージョンを安全に拒否する。

### 3.3 Algorithm

暗号方式を識別するフィールドを持つ。

初期バージョンではAES-256-GCMのみをサポートする。

例:

```text
Algorithm = AES-256-GCM
```

将来的に別方式を追加できるよう、文字列ではなく固定値のenum/ID等を検討する。

### 3.4 KDF

KDF方式を識別する。

初期バージョンではArgon2idのみ。

### 3.5 KDFパラメータ

Argon2idのパラメータを暗号ファイルに保存する。

少なくとも以下を保持する。

- memory
- iterations/time
- parallelism
- salt

これにより、将来KDFパラメータを変更しても過去ファイルを復号できる。

### 3.6 Salt

暗号化ごとに暗号学的乱数で生成する。

同じパスワードで同じファイルを暗号化しても、可能な限り同一の暗号文にならないようにする。

### 3.7 Nonce

AES-GCMでnonceを再利用しない。

暗号化時に安全な乱数からnonceを生成し、必要な情報をファイル内に保存する。

同一鍵でnonceを再利用する実装は禁止する。

### 3.8 Original file extension

暗号化前のファイル拡張子を保存する。

---

## 4. ヘッダーの認証

ヘッダーのうち、暗号方式・KDF・パラメータなどの暗号処理に影響する情報について、改ざんを検出できるようにする。

AES-GCMのAAD（Additional Authenticated Data）としてヘッダーを認証する設計を基本とする。

目的:

```text
Algorithm
Version
KDF parameters
Salt
Nonce
Original file extension
```

等を暗号ファイル作成後に改ざんされても検出できるようにする。

---

## 5. 暗号化/復号化の自動判定

`akko` は入力ファイルの先頭を読み、Magicを検査する。

### 通常ファイル

```text
Magicなし
    |
    v
暗号化
```

### akkoファイル

```text
Magicあり
    |
    v
ヘッダー検証
    |
    v
復号化
```

したがって、

```text
akko --key key secret.txt
```

は暗号化、

```text
akko --key key secret.akko
```

は復号化となる。

### 重要

Magicが一致しただけで「復号可能」と判断してはいけない。

以下を区別する。

1. akko形式ではない
2. akko形式だがフォーマットが壊れている
3. akko形式だが未対応バージョン
4. akko形式だが未対応アルゴリズム
5. akko形式で鍵が間違っている
6. akko形式だが暗号文が改ざん/破損している
7. 正常に復号できた

特に「鍵が間違っている」と「ファイルが破損している」は、AES-GCMの認証失敗として安全に処理する。

---

## 6. ストリーミング対応

巨大ファイルをメモリに丸ごと読み込まない。

以下のようなストリーミング設計を採用する。

```text
input
  |
  +-- chunk 1 --> encrypt --> output
  +-- chunk 2 --> encrypt --> output
  +-- chunk 3 --> encrypt --> output
  ...
```

AES-GCMをファイル全体に対して1回だけ適用するのではなく、必要に応じてチャンク単位のAEADを検討する。

### チャンク形式の候補

```text
Header

Chunk length
Chunk ciphertext + authentication tag

Chunk length
Chunk ciphertext + authentication tag

...
```

各チャンクは独立したAEADとして認証する。

チャンクごとのnonce再利用を絶対に起こさない設計にする。

具体的なnonce生成方式は実装時に決定する。

例えば、暗号化開始時にランダムなnonce baseを生成し、チャンク番号等から各チャンクのnonceを一意に生成する方式を検討する。

---

## 7. 出力ファイルの安全な生成

復号時にいきなり既存の出力ファイルを上書きしない。

推奨フロー:

```text
temporary fileへ出力
        |
        v
最後まで正常に復号
        |
        v
認証成功
        |
        v
atomic rename
        |
        v
最終出力
```

これにより、間違った鍵や破損した暗号ファイルによって、既存の復号先ファイルが壊れることを防ぐ。

暗号化時も同様の安全な出力方法を検討する。

---

## 8. 入力/出力の事故防止

以下を明示的にチェックする。

- inputとoutputが同一パスではないこと
- inputが存在すること
- outputの親ディレクトリが存在すること
- outputが既に存在する場合は基本的に上書きしないこと
- ただし、`--force` オプションで上書きを許可することができる
- ディレクトリを入力された場合はエラーにすること（将来的にディレクトリが指定された場合は警告を表示して、y/nをプロンプトに表示して確認、または `--directory` オプションで対応）
- 特殊ファイルの扱い（FIFO, socket, device fileなど）はエラーにする
- ファイル権限/permissionの扱い（v1後に対応済み。19.3参照）

---

## 9. 再暗号化について

再暗号化とは、既に暗号化されたファイルを、別のパスワードで再度暗号化することを指す。
この機能はサポートしない。

---

## 10. CLI仕様

初期仕様:

```text
akko --key <key> <input>
```

例:

```text
akko --key key secret.txt
akko --key key secret.akko
```

`key` の意味は「鍵情報」とする。

```text
akko <input>
Password:
```

例:

```text
akko secret.txt
Password:
akko secret.akko
Password:
```

また、stdin/stdout対応も将来的な候補とする。

---

## 11. エラー設計

エラーはユーザーが原因を理解できる程度に明確にする。

例:

```text
input file does not exist
output file already exists
invalid akko file
unsupported akko version
unsupported encryption algorithm
unsupported KDF
authentication failed
invalid password or corrupted file
```

ただし、攻撃者に不要な情報を与えないよう、鍵の正否と暗号文改ざんの詳細を過剰に区別しない。

---

## 12. セキュリティ要件

最低限、以下を満たす。

- 独自暗号アルゴリズムを実装しない
- AES-256-GCM等の実績あるAEADを使用する
- パスワードを直接暗号鍵にしない
- Argon2id等のKDFを使用する
- saltを暗号化ごとにランダム生成する
- AES-GCM nonceを再利用しない
- 認証タグを必ず検証する
- ヘッダーも改ざん検出の対象にする
- 復号途中の不完全な出力を最終ファイルとして残さない
- エラー時に秘密情報をログ出力しない
- パスワードを通常ログに出さない
- 暗号化済みファイルを二重暗号化しない

---

## 13. テスト

### 13.1 基本テスト

- 小さいテキストファイルの暗号化→復号
- バイナリファイルの暗号化→復号
- 空ファイル
- 1 byteのファイル
- 大きなファイル
- 日本語ファイル名
- 長いファイル
- ランダムデータ

### 13.2 鍵関連

- 正しい鍵で復号できる
- 間違った鍵では復号できない
- 空パスワードの扱い
- 長いパスワード
- Unicodeパスワード

### 13.3 改ざんテスト

暗号ファイルについて、

- Magicを変更
- Versionを変更
- Algorithmを変更
- KDFパラメータを変更
- Saltを変更
- Nonceを変更
- ciphertextを変更
- authentication tagを変更

した場合、適切に検出できること。

### 13.4 nonce/salt

複数回同じ入力・同じパスワードで暗号化し、salt/nonceが適切に変化することを確認する。

同じ入力から同一の暗号ファイルが生成されないことを確認する。

### 13.5 入出力事故

- input == output
- outputが既に存在
- 出力先ディレクトリが存在しない
- 権限不足
- 復号途中でエラー
- ディスク容量不足

などをテストする。

---

## 14. プロジェクト構成案

Goのプロジェクト構成は、例えば以下を基本とする。

```text
akko/
├── go.mod
├── go.sum
├── README.md
├── plan.md
├── Taskfile.yml
├── cmd/
│   └── akko/
│       └── main.go
├── internal/
│   ├── crypto/
│   │   ├── kdf.go
│   │   ├── aead.go
│   │   └── crypto_test.go
│   ├── format/
│   │   ├── header.go
│   │   ├── format.go
│   │   └── format_test.go
│   ├── encrypt/
│   │   ├── encrypt.go
│   │   └── encrypt_test.go
│   ├── decrypt/
│   │   ├── decrypt.go
│   │   └── decrypt_test.go
│   └── cli/
│       ├── cli.go
│       └── cli_test.go
└── testdata/
```


---

## 15. 実装順序

以下の順番で実装する。

### Phase 1: フォーマット設計

- Magic
- Version
- Algorithm ID
- KDF ID
- KDF parameters
- Salt
- Nonce
- ciphertext
- authentication tag
- ヘッダーのシリアライズ/デシリアライズ

を具体的に決定する。

この段階でバイトレベルのフォーマットをREADMEまたは別ドキュメントに記載する。

### Phase 2: KDF

Argon2idによるパスワード→32 byte鍵生成を実装する。

### Phase 3: AEAD

AES-256-GCMによる暗号化/復号化を実装する。

AADとしてヘッダーを認証できる構造にする。

### Phase 4: ファイルI/O

ストリーミング暗号化・復号化を実装する。

### Phase 5: 自動判定

Magicを利用して、

```text
通常ファイル -> encrypt
akko file -> decrypt
```

を実装する。

### Phase 6: 安全な出力

temporary file + atomic rename等を実装する。

### Phase 7: CLI

```text
akko --key <key> <input>

akko <input>
Password:
```

を実装する。

### Phase 8: テスト

単体テスト、統合テスト、改ざんテスト、エラーケースを追加する。

### Phase 9: README

以下を説明する。

- インストール
- 基本的な使い方
- 暗号化
- 復号
- ファイルフォーマット
- セキュリティ上の注意
- パスワードをCLI引数に指定することのリスク
- 互換性/versioning

---

## 16. 実装上の重要事項

### 絶対に避けること

以下は実装しない。

```text
自作暗号アルゴリズム
単純なXOR暗号
AES-ECB
パスワードをそのままAES鍵にする
固定salt
固定nonce
AES-GCM nonceの再利用
認証タグを無視する
暗号化されたデータをメモリに全読み込みする
復号途中のデータを直接最終ファイルへ書き込む
```

### 判断に迷った場合

暗号仕様を独自に発明するより、Goの実績ある暗号ライブラリ/APIの設計に合わせる。

また、セキュリティ上重要な部分については、実装前に設計を明文化する。

---

## 17. 完成条件

以下を満たした時点でv1完成とする。

```text
[ ] 通常ファイルを暗号化できる
[ ] akkoファイルを自動判定できる
[ ] 正しい鍵で復号できる
[ ] 間違った鍵で復号できない
[ ] 改ざんを検出できる
[ ] saltがランダムである
[ ] nonceが安全に生成される
[ ] Argon2idを使用している
[ ] AES-256-GCMを使用している
[ ] ヘッダーが認証されている
[ ] 大きなファイルをストリーミング処理できる
[ ] 復号失敗時に既存ファイルを破壊しない
[ ] 暗号化済みファイルの二重暗号化を防止する
[ ] フォーマットにVersionがある
[ ] 単体テスト・統合テストがある
[ ] READMEに仕様と制約を記載している
```

## 18. エージェントへの注意

このplan.mdを実装タスクとして使用する場合、暗号フォーマットの細部について要件から勝手に都合のよい仕様を作らず、まず `format` の設計を確定する。

特に以下は実装前に確認・設計すること。

1. ヘッダーの正確なバイトレイアウト
2. AEADのAADに含める範囲
3. ストリーミング時のチャンク構造
4. チャンクごとのnonce生成方法
5. Argon2idの初期パラメータ
6. Version/Algorithm/KDFのID体系
7. output上書きポリシー
8. 二重暗号化時の挙動

暗号処理については「動けばよい」ではなく、nonce再利用・認証失敗・改ざん・途中書き込み・鍵導出などのセキュリティ上の問題を優先してレビューする。

---

## 19. v1後の追加機能（フィードバック反映）

v1完成後、実際の利用フィードバックを受けて以下を追加実装した。

### 19.1 複数ファイル/ワイルドカード対応

- `akko sample_*.txt` のように、入力にワイルドカードパターンを指定できる。
- シェルが展開しない環境でも動作するよう、`filepath.Glob` により `akko` 自身が展開する（シェルが先に展開した場合はそのまま複数の通常引数として扱う）。
- マッチした各ファイルは、暗号化・復号化を個別に自動判定して処理する。1件失敗しても他のファイルの処理は継続し、1件でも失敗があれば終了コードは非ゼロになる。
- `--output`/`-o` は、対象が複数ファイルになる場合は指定できない（出力先が一意に決まらないため）。
- 事故防止のため、引数に `*` `?` `[` などのワイルドカード文字が含まれる場合は、対象ファイル一覧を表示し `y/N` で確認を求める（`y`/`yes`以外は中断）。シェルが`*`等を事前に展開して引数にワイルドカード文字が残らない場合でも、解決後のファイルが複数になる場合は同様に確認する。

### 19.2 アーカイブ機能（`--archive`）

- `--archive` を指定すると、暗号化対象の複数ファイルを1つのtarにまとめてから暗号化し、単一の `.akko` ファイルを出力する。
 - アーカイブかどうかはヘッダーの `OriginalExt` に予約値（`.akkoarchive`）を保存することで表現し、AES-GCMのAAD認証の対象にもなる。
 - 復号時はヘッダーから自動判定するため、`--archive` を付けなくても自動的にアーカイブ展開される（`--archive`は暗号化時にのみ意味を持つ）。
 - アーカイブ内のファイル名は指定時の相対パス（先頭の`/`や`..`を除去）を保持するため、サニタイズ後に生成されるパス名が重複する（衝突する）場合に暗号化時エラーになる。
 - 入力に既に `.akko` ファイルが含まれる場合、既存の akko ファイルはアーカイブに含めず個別に復号処理される（`--archive` は通常ファイルをまとめて暗号化する動作にのみ適用される）。
 - 展開時はパストラバーサル（`..`や絶対パスのエントリ）を拒否する。

### 19.3 ファイルパーミッションの維持

- ヘッダーに `OriginalMode`（4 byte、`NonceBase`と拡張子の間に配置、認証対象）を追加し、暗号化時に元ファイルのパーミッションを保存する。
- 復号時に `os.Chmod` でパーミッションを復元する。`--archive` でまとめた場合もアーカイブ内の各ファイルごとに個別のパーミッションを保存・復元する。
- ヘッダーレイアウト変更に伴い、`internal/akko/format.go` の `marshal`/`unmarshal` と、改ざん検出テスト（`tamper_test.go`）のバイトオフセットを更新済み。

### 19.4 Windows/Mac/Linux間の互換性

- 暗号ファイル形式自体（アルゴリズム・KDF・固定長バイナリレイアウト）はOS/CPUアーキテクチャに依存しないため、異なるOS間で暗号化・復号を行っても問題ない。キーファイルの改行コード（`\r\n` / `\n`）もどちらも許容する。
- ただし、19.3のパーミッション維持はUnix系のrwxビットをそのまま保存・復元する設計のため、Windowsでは意味が異なる（`os.Chmod`は読み取り専用属性のON/OFFしか制御できない）。異なるOS間でパーミッションが完全に再現されることは期待できない。
